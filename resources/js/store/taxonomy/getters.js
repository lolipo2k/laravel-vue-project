export function fields(state) {
    return state.fields;
}

export function fieldCategories(state) {
    return state.fieldCategories;
}

export function regions(state) {
    return state.regions;
}

export function workExperience(state) {
    return state.workExperience;
}
